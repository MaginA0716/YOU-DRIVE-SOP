# ADR 025: 全自动 AI 登舰加固与物理入口收拢

## 状态 (Status)
已通过 (Accepted)

## 上下文 (Context)
在实际运行中发现，即便存在治理文档，AI 引擎和新用户仍可能因未及时读取关键规约而导致“智力断档”。现有的引导方式过于依赖用户的自觉性，缺乏物理层面的强制触发机制。

## 决策 (Decision)
1.  **物理入口收拢**：在 `README.md` 顶部建立醒目的“AI 代理登舰区”，并创建跨 AI 通用的 `AGENTS.md` 握手协议。
2.  **开机脚本加固**：重塑 `GEMINI.md`，注入 `[CRITICAL-BOOT-SEQUENCE]`，利用 CLI 自动加载特性强制 AI 启动自检。
3.  **视觉化闭环**：在 `SOP_CORE_MANUAL.md` 中引入“智力资产全景循环图”，通过可视化路径消除流程认知模糊。

## 后果 (Consequences)
- **正面**：实现了“进入即握手，启动即对齐”的零干扰协作；彻底解决了多会话场景下的规约遗忘问题。
- **负面**：增加了根目录的文件密度，需保持 `AGENTS.md` 等文件的极简性以减少上下文损耗。

---
*Created on 2026-03-14 by YOU-DRIVE-SOP Engine.*
